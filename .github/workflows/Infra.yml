name: 'Terraform'

on:
  push:
    branches: [ main ]
      
jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
  
  steps:
  - name: Terraform Init
  - uses: hashicorp/setup-terraform@v2
    with:
      terraform_wrapper: false