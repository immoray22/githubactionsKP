apiVersion: v1
kind: Pod
metadata:
  name: demo-kotlinapp
  labels:
    role: webapp
spec:
  containers:
    - name: web
      image: immoray22/demo-kotlinimran
      ports:
        - containerPort: 80
          name: web
          protocol: TCP